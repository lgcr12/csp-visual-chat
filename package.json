{
  "name": "csp-visual-chat",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --use-env-proxy server.js",
    "dev": "node --use-env-proxy --watch server.js",
    "deploy": "powershell -ExecutionPolicy Bypass -File deploy.ps1"
  },
  "engines": {
    "node": ">=20"
  }
}
